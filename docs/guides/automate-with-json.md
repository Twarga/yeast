# Automate With JSON

Use `--json` when scripting Yeast.

```bash
yeast status --json
yeast inspect web --json
```

Use `--json --events` for lifecycle progress:

```bash
yeast up --json --events
```

Human output is for people. JSON output is for tools.
